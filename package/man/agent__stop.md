#### Description

The `stop` command stops the agent services. Call this when done to clean up background processes.

#### Usage

```bash
aux4 agent stop [--queuePort <port>]
```

--queuePort  Queue server port (default: 8420)

#### Example

```bash
aux4 agent stop
```

```text
Agent stopped
```
